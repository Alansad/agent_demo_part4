# Anthropic API Key（必填）
LLM_API_KEY=xxx

# 自定义 API 地址（可选，留空则使用官方地址）
# 支持任何 Anthropic 格式的中转服务，如：
# LLM_BASE_URL=https://api.your-proxy.com
LLM_BASE_URL=xxx

# 默认模型（可在前端界面覆盖）
LLM_MODEL=xxx

# 服务端口
PORT=3000